question
Given an array arr. Modify the elements of the array to square each even element and cube each odd element.

Write a function called powerArray that takes an array arr, squares every even element, cube every odd element, and returns the modified array.

solution:


function powerArray(arr){
let newArr = [];
  for (let i = 0; i < arr.length; i ++) {
    if(arr[i] % 2 === 0)
    newArr.push(Math.pow(arr[i],2));
      else
        newArr.push(Math.pow(arr[i],3));

   }
  return newArr;
}
