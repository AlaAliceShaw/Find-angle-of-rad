question
Write a function integerPartsOfElements that takes an arr array as an argument and returns an array 
in which the elements are equal to the integer part of their value.
Examples:
function integerPartsOfElements ([7.23, 34.89, 0.9, 4.45, 8.5]) should return [7, 34, 0, 4, 8]


solution:

function integerPartsOfElements(arr){
  let x = 0;
  let len = arr.length;
  while(x < len){
  arr[x] = Math.trunc(arr[x]);
  x++;
  }
return arr;
}
