question
Find the sum of the squares of numbers from 1 to n:
sumOfsquares


solution:

function sumOfSquares(n){
  let sum = 0;
    for(let i = 1; i<= n; i++){
      sum += Math.pow(i,2)
    }
   return sum;
}
